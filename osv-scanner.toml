## This repository previously ignored Scorecard findings tied to a test-only
## Java archive. The artifact is no longer present in-tree, so we keep the
## configuration file but with no repository-level vulnerability suppressions.
